model User {
    id        String    @id @unique @default(uuid())
    username  String
    firstname String
    lastnamee String
    email     String
    posts     Post[]
    accounts  Account[]
    sessions  Session[]
}
